# deployments/urandom/.env
#
# Environment variables for the urandom gRPC entropy profile.
# Copy this file to .env and edit as needed:
#   cp .env.example .env
#
# Most users only need to touch HF_MODEL, HF_TOKEN, and the host ports below.
# The QR_* values here are already the recommended baseline for this profile.

# --- Hugging Face ---
# Model to serve. Default: Qwen/Qwen2.5-1.5B-Instruct
HF_MODEL=Qwen/Qwen2.5-1.5B-Instruct
# Set this if the model is gated (requires accepting a license).
HF_TOKEN=

# --- Entropy source ---
# The urandom profile uses a co-located gRPC entropy server.
QR_ENTROPY_SOURCE_TYPE=quantum_grpc
QR_GRPC_SERVER_ADDRESS=entropy-server:50051
QR_FALLBACK_MODE=system

# --- Sampling parameters ---
QR_TEMPERATURE_STRATEGY=fixed
QR_FIXED_TEMPERATURE=0.7
QR_TOP_K=0
QR_TOP_P=1.0
QR_LOG_LEVEL=summary

# --- Optional advanced settings (defaults shown) ---
# QR_GRPC_MODE=unary
# QR_GRPC_TIMEOUT_MS=5000
# QR_GRPC_METHOD_PATH=/qr_entropy.EntropyService/GetEntropy
# QR_GRPC_STREAM_METHOD_PATH=/qr_entropy.EntropyService/StreamEntropy
# QR_SAMPLE_COUNT=20480

# --- Ports (host-side) ---
VLLM_PORT=8000
ENTROPY_SERVER_PORT=50051

# --- Open WebUI (optional, --profile ui) ---
OPEN_WEBUI_PORT=3000
# Set to true to require login (recommended for shared/public servers).
OPEN_WEBUI_AUTH=false
