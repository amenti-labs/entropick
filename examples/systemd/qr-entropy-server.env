# Environment file for qr-entropy-server.service
#
# Copy to /etc/default/qr-entropy-server and adjust as needed.
# Restrict permissions: sudo chmod 600 /etc/default/qr-entropy-server

# Which server script to run.
# Options:
#   /opt/entropick/examples/servers/simple_urandom_server.py  (default)
#   /path/to/your/custom_server.py
ENTROPY_SERVER_SCRIPT=/opt/entropick/examples/servers/simple_urandom_server.py

# Listen address. Use 0.0.0.0:PORT for network access, or
# unix:///var/run/qrng.sock for local-only (lowest latency).
LISTEN_ADDRESS=0.0.0.0:50051

# Thread pool size for handling concurrent gRPC requests.
MAX_WORKERS=4

# Hardware device path (used by qrng_template_server.py).
# QRNG_DEVICE=/dev/qrng0
